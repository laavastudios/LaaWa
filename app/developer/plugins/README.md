# Plugin Center

The Plugin Center is the operator UI for the external extension gateway. It is intentionally separate from the Next.js application runtime: plugins are HTTPS services that receive signed events.

The UI exposes installation, permission/event selection, enable/disable, test delivery, and uninstall controls.
