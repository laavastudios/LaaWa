# API Maintenance

Keep the live OpenAPI contract and written documentation aligned whenever an endpoint, request body, response envelope, authentication scope, or event type changes.

Prefer additive, versioned API changes. Update examples and deployment/security guidance when behavior changes. Run lint and production build checks before merging API changes.