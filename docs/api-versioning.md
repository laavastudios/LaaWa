# API Versioning

The current developer API is `v1` and is addressed under `/api/v1`.

Integrations should treat the versioned path as the compatibility boundary. Use the OpenAPI contract served by the same deployment to discover the supported surface.