# API Changelog

## v1

The initial public developer surface includes:

- API capability and health endpoints
- Scoped API key management
- Message reads and writes
- Conversation listing and history
- Contact search
- Authenticated realtime SSE events
- OpenAPI 3.0.3 contract

The API uses standard HTTP semantics, versioned paths, machine-readable errors, request identifiers, and workspace/account isolation.