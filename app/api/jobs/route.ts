import { NextResponse } from "next/server";
import { cookies } from "next/headers";
import { verifySession } from "../auth/login/route";
import { isDatabaseConfigured, query, withTransaction } from "../../../lib/db";
export const dynamic = "force-dynamic";
export const runtime = "nodejs";
async function auth() { const store = await cookies(); return verifySession(store.get("laawa_session")?.value); }
async function workspace() { const result = await query<{ id: string }>("SELECT id FROM workspaces ORDER BY created_at ASC LIMIT 1"); return result.rows[0]?.id || null; }
export async function GET() { if (!(await auth())) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); if (!isDatabaseConfigured()) return NextResponse.json({ jobs: [], schedules: [], configured: false }); try { const ws = await workspace(); if (!ws) return NextResponse.json({ jobs: [], schedules: [], configured: true }); const [jobs, schedules, stats] = await Promise.all([query("SELECT id,type,status,attempts,max_attempts,run_at,last_error,completed_at,created_at FROM jobs WHERE workspace_id=$1 ORDER BY run_at DESC LIMIT 100", [ws]), query("SELECT s.id,s.name,s.body,s.run_at,s.next_run_at,s.timezone,s.recurrence,s.status,s.last_job_id,wa.name AS account_name,c.chat_id FROM scheduled_jobs s LEFT JOIN whatsapp_accounts wa ON wa.id=s.whatsapp_account_id LEFT JOIN conversations c ON c.id=s.conversation_id WHERE s.workspace_id=$1 ORDER BY COALESCE(s.next_run_at,s.run_at) ASC LIMIT 100", [ws]), query("SELECT COUNT(*)::int AS total, COUNT(*) FILTER (WHERE status='queued')::int AS queued, COUNT(*) FILTER (WHERE status='running')::int AS running, COUNT(*) FILTER (WHERE status='completed')::int AS completed, COUNT(*) FILTER (WHERE status='failed')::int AS failed FROM jobs WHERE workspace_id=$1", [ws])]); return NextResponse.json({ jobs: jobs.rows, schedules: schedules.rows, stats: stats.rows[0], configured: true }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to load jobs." }, { status: 500 }); } }
export async function POST(request: Request) { if (!(await auth())) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); if (!isDatabaseConfigured()) return NextResponse.json({ error: "DATABASE_URL is required for scheduling." }, { status: 503 }); try { const body = await request.json(); const name = String(body?.name || "Scheduled message").trim().slice(0,120); const message = String(body?.body || "").trim(); const runAt = new Date(String(body?.runAt || "")); const recurrence = ["none","daily","weekly"].includes(body?.recurrence) ? body.recurrence : "none"; const timezone = String(body?.timezone || "UTC").trim(); const accountId = String(body?.whatsappAccountId || "").trim(); const conversationId = String(body?.conversationId || "").trim(); if (!message || Number.isNaN(runAt.getTime()) || runAt.getTime() <= Date.now() || !conversationId || !accountId) return NextResponse.json({ error: "Account, conversation, message and a future run time are required." }, { status: 400 }); const ws = await workspace(); if (!ws) return NextResponse.json({ error: "Workspace is not initialized." }, { status: 503 }); const result = await query("INSERT INTO scheduled_jobs (workspace_id,whatsapp_account_id,conversation_id,name,body,run_at,next_run_at,timezone,recurrence) SELECT $1,$2,$3,$4,$5,$6,$6,$7,$8 WHERE EXISTS (SELECT 1 FROM whatsapp_accounts WHERE id=$2 AND workspace_id=$1) AND EXISTS (SELECT 1 FROM conversations WHERE id=$3 AND workspace_id=$1) RETURNING *", [ws, accountId, conversationId, name, message, runAt.toISOString(), timezone, recurrence]); if (!result.rows[0]) return NextResponse.json({ error: "Account or conversation was not found in this workspace." }, { status: 404 }); return NextResponse.json({ schedule: result.rows[0] }, { status: 201 }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to create schedule." }, { status: 500 }); } }
export async function PATCH(request: Request) { if (!(await auth())) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); if (!isDatabaseConfigured()) return NextResponse.json({ error: "DATABASE_URL is required." }, { status: 503 }); try { const body = await request.json(); const id = String(body?.id || "").trim(); const action = String(body?.action || ""); const ws = await workspace(); if (!id || !ws) return NextResponse.json({ error: "Schedule id is required." }, { status: 400 }); if (action === "cancel") { const result = await query("UPDATE scheduled_jobs SET status='cancelled',next_run_at=NULL,updated_at=NOW() WHERE id=$1 AND workspace_id=$2 RETURNING id", [id,ws]); return result.rows[0] ? NextResponse.json({ ok:true }) : NextResponse.json({ error:"Schedule not found." },{status:404}); } if (action === "reschedule") { const runAt = new Date(String(body?.runAt || "")); if (Number.isNaN(runAt.getTime()) || runAt.getTime() <= Date.now()) return NextResponse.json({ error:"A future run time is required." },{status:400}); const result = await query("UPDATE scheduled_jobs SET run_at=$3,next_run_at=$3,status='scheduled',last_job_id=NULL,updated_at=NOW() WHERE id=$1 AND workspace_id=$2 RETURNING *",[id,ws,runAt.toISOString()]); return result.rows[0] ? NextResponse.json({ schedule:result.rows[0] }) : NextResponse.json({error:"Schedule not found."},{status:404}); } if (action === "retry") { const result = await query("UPDATE jobs SET status='queued',run_at=NOW(),last_error=NULL,locked_at=NULL,locked_by=NULL,updated_at=NOW() WHERE id=$1 AND workspace_id=$2 AND status='failed' RETURNING id",[id,ws]); return result.rows[0] ? NextResponse.json({ok:true}) : NextResponse.json({error:"Failed job not found."},{status:404}); } return NextResponse.json({error:"Unsupported job action."},{status:400}); } catch(error){ return NextResponse.json({error:error instanceof Error?error.message:"Unable to update job."},{status:500}); } }
export async function DELETE(request: Request) { if (!(await auth())) return NextResponse.json({error:"Unauthorized"},{status:401}); if (!isDatabaseConfigured()) return NextResponse.json({error:"DATABASE_URL is required."},{status:503}); const ws=await workspace(); const id=new URL(request.url).searchParams.get("id"); if(!ws||!id) return NextResponse.json({error:"Schedule id is required."},{status:400}); const result=await query("DELETE FROM scheduled_jobs WHERE id=$1 AND workspace_id=$2 RETURNING id",[id,ws]); return result.rows[0]?NextResponse.json({ok:true}):NextResponse.json({error:"Schedule not found."},{status:404}); }