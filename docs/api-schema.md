# API Schema

The authoritative schema is generated and served from the deployed application at `/api/v1/openapi.json`.

Do not maintain a second manually generated API contract. Client tooling should import the live OpenAPI document from the same LaaWa deployment it targets.