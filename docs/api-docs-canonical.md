# Canonical API Docs

The canonical machine-readable API contract is `/api/v1/openapi.json`.

The canonical interactive entry point is `/developer/docs/guide`.

The canonical written reference is `docs/api-reference.md`.